/**
 * Enumeración que define los roles de usuario en el sistema.
 */
export enum Role {
    /**
     * Rol de administrador con acceso completo al sistema.
     */
    ADMIN = "Admin",

    /**
     * Rol de usuario con acceso limitado al sistema.
     */
    USER = "User",
}
